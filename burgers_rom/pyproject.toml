[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "burgers_rom"
version = "0.1.0"
dependencies = [
    "numpy",
    "pysindy",
    "pyyaml",
    "dynabench",
    "cvxpy>=1.7.5",
]

[tool.setuptools]
packages = ["burgers_rom"]
